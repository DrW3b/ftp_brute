# ftp brute-force tool
-please use it only for ethical purposes-

* install: git clone https://github.com/DrW3b/ftp_login.git && cd ftp_login && pip3 install -r requirments.txt
* use: python3 ftp_login.py -t <targets.txt> -u <usernames.txt> -p <passwords.txt>
